export enum LogType {
  LOG = 'log',
  INFO = 'info',
  WARN = 'warn',
  ERROR = 'error',
  DEBUG = 'debug',
  FATAL = 'fatal',
  VERBOSE = 'verbose',
}

export enum LogLabel {
  RabbitMQ = 'RabbitMQ',
  Redis = 'Redis',
  Http = 'Http',
}
